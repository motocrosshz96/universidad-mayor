<%- include("template/cabecera", { tituloweb: "Editar Mascota" }) %>

<div class="container mt-4">
    <h2>Editar Mascota</h2>
    <form action="/mascotas/<%= mascota._id %>?_method=PUT" method="POST">
        <div class="form-group">
            <label>Nombre</label>
            <input type="text" name="nombre" class="form-control" value="<%= mascota.nombre %>" required>
        </div>
        <div class="form-group">
            <label>Descripción</label>
            <input type="text" name="descripcion" class="form-control" value="<%= mascota.descripcion %>" required>
        </div>
        <button type="submit" class="btn btn-primary mt-3">Actualizar</button>
        <a href="/mascotas" class="btn btn-secondary mt-3">Cancelar</a>
    </form>
</div>

<%- include("template/footer") %>
