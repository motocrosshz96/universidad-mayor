<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1" />
<title><%= tituloweb %></title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<style>
    /* Body y fuente */
    body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #fffaf6;
    color: #4a4a4a;
    margin: 0;
    padding: 0;
    }

    /* Header */
    header {
    background-color: #ff914d;
    box-shadow: 0 3px 8px rgba(0, 0, 0, 0.15);
    padding: 1rem 0;
    position: sticky;
    top: 0;
    z-index: 100;
    }

    header h1 {
    font-weight: 700;
    font-size: 1.8rem;
    letter-spacing: 1.5px;
    }

    /* Navegación */
    nav ul.nav {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    gap: 1.5rem;
    }

    nav ul.nav li.nav-item a.nav-link {
    color: #fff;
    font-weight: 600;
    font-size: 1rem;
    padding: 0.5rem 0.75rem;
    border-radius: 8px;
    transition: background-color 0.3s ease, transform 0.2s ease;
    text-decoration: none;
    display: inline-block;
    }

    nav ul.nav li.nav-item a.nav-link:hover,
    nav ul.nav li.nav-item a.nav-link:focus {
    background-color: rgba(255, 255, 255, 0.3);
    transform: scale(1.1);
    text-decoration: none;
    outline: none;
    }

    /* Botones generales */
    .btn {
    font-weight: 600;
    border-radius: 50px;
    padding: 0.45rem 1.5rem;
    transition: background-color 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease;
    box-shadow: 0 3px 6px rgba(0, 0, 0, 0.12);
    user-select: none;
    cursor: pointer;
    }

    .btn:hover,
    .btn:focus {
    transform: translateY(-3px);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
    outline: none;
    }

    /* Colores para botones específicos */
    .btn-success {
    background-color: #ff914d;
    border: 1.5px solid #ff914d;
    color: white;
    }

    .btn-success:hover {
    background-color: #ff6f00;
    border-color: #ff6f00;
    }

    .btn-info {
    background-color: #3db9d3;
    border: 1.5px solid #3db9d3;
    color: white;
    }

    .btn-info:hover {
    background-color: #25a0bb;
    border-color: #25a0bb;
    }

    .btn-danger {
    background-color: #f04c4c;
    border: 1.5px solid #f04c4c;
    color: white;
    }

    .btn-danger:hover {
    background-color: #d93939;
    border-color: #d93939;
    }

    /* Footer */
    footer {
    background-color: #ffe3cf;
    padding: 1rem 0;
    text-align: center;
    color: #6a4c2b;
    font-weight: 600;
    box-shadow: inset 0 1px 3px rgba(0, 0, 0, 0.05);
    }
</style>
</head>
<body>

<header class="text-white container">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center">
        <h1 class="mb-3 mb-md-0">Veterinaria Trébol 🐾</h1>       
        <nav>
            <ul class="nav">
                <li class="nav-item"><a class="nav-link" href="/">Inicio</a></li>
                <li class="nav-item"><a class="nav-link" href="/mascotas">Mascotas</a></li>
                <li class="nav-item"><a class="nav-link" href="/mascotas/agregar">Agregar</a></li>
                <li class="nav-item"><a class="nav-link" href="/usuarios/registro">Registro</a></li>
                <li class="nav-item"><a class="nav-link" href="/usuarios/login">Iniciar sesión</a></li>
            </ul>
        </nav>
    </div>

    <% if (session && session.usuario) { %>
        <div class="mt-3 d-flex justify-content-between align-items-center">
            <p class="mb-0 text-dark fw-bold">👋 Hola, <%= session.usuario.nombre %></p>
            <a href="/usuarios/logout" class="btn btn-danger ms-3">Cerrar sesión</a>
        </div>
    <% } %>
</header>


<main class="container py-4">

